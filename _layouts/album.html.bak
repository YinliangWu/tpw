--- 
layout: post 
comments: true
--- 
 
  
  
        	{% for photo in page.photos %}
          
                <img alt="{{ photo.alt }}"  src="{{ photo.url }}" class="img-fluid">
                <div class="text">{{ photo.label }}</div>
             
          {% endfor %}           
        
    
